const { MessageEmbed } = require("discord.js")
const config = require("../../config.json")
module.exports.run = async (message, bot, args, idioma) => {
    let embed = new MessageEmbed()
    embed.setTitle("Changelog")
    embed.
}